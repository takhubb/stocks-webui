services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      JQUANTS_CACHE_DIR: /app/cache/jquants
      JQUANTS_BULK_MONTHS: 18
    volumes:
      - ./cache:/app/cache
